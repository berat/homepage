const SectionTitle = ({ title }: { title: string }) => {
  return (
    <h6 className="uppercase text-gray text-sm tracking-wider font-extrabold mt-6">{title}</h6>
  );
};

export default SectionTitle;
